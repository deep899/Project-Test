---
deployment:
  tasks:
    - export DEPLOYPATH=/home/infinitybrains/public_html/
    - /bin/chmod 755 ${DEPLOYPATH}
    - /bin/find ${DEPLOYPATH} -type d -exec /bin/chmod 755 '{}' \;
    - /bin/find ${DEPLOYPATH} -type f -exec /bin/chmod 644 '{}' \;
    - /bin/cp -R dist/spa* $DEPLOYPATH

    
